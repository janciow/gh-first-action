name: First workflow
on: [workflow_dispatch, push]
jobs:
  firt-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print geeedeng
        run: echo "Hello"
      - name: goodby
        run: echo "Bay done"
        
