name: First workflow
on: 
  - workflow_dispatch
  - push
  #- schedule:
   # - cron: ' 
jobs:
  first-job:
    runs-on: ubuntu-latest
    name: job1
    steps:
      - name: Runnig first job
        run: echo "hi shell"
      - name: check os relies
        run: cat /etc/os-release
      - name: good bay
        run: echo "Bay done"
      - name: not bay
        run: |
          echo "bay none"
          sleep 15
  next-job-2:
    runs-on: ubuntu-latest
    name: job2
    steps:
      - name: checking host name
        run: hostname
      - name: run mutiple comands
        run: |
          echo "hi 1"
          echo "hi 2 'date'"   
          sleep 10
  next-job-3:
    name: job3
    needs: [ next-job-2] 
    runs-on: ubuntu-latest
    steps:
      - name: checking host name
        run: hostname
      - name: run mutiple comands
        run: |
          echo "hi 1"
          echo "hi 2 'date'"   
          sleep 10

  next-job-4:
    name: job4
    needs: [first-job, next-job-3, next-job-2] 
    runs-on: ubuntu-latest
    steps:
      - name: checking host name
        run: hostname
      - name: run mutiple comands
        run: |
          echo "hi 1"
          echo "hi 2 'date'"   
          sleep 10
